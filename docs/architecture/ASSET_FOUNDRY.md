# World Asset Foundry Seam (G12F)

`wanxiang_substrate.assets` defines SemanticSceneSpec -> AssetCandidate via
generator/scan/reconstruction ports, geometry validation and semantic binding.
The foundry produces candidates only; it never owns world truth.