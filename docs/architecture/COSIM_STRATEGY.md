# Co-Simulation & Strategy (G11E/G11F)

Batch experiments across scenario/policy variants produce distributions with
verification/validation and ValidityEnvelope metadata (G11E). The Liaoshen
real historical pack is EXTERNAL_BLOCKED (no rights-cleared corpus); the
synthetic campaign domain and manifest template are committed, and missing
history is never fabricated (G11F).