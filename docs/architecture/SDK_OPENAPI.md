# Package SDK + OpenAPI / TypeScript SDK (G12C)

`wanxiang_substrate.packages.sdk` provides authoring helpers
(`validate_manifest_for_authoring`) and documents the public API vocabulary +
version policy. `packages/sdk_ts` parses a stable OpenAPI document into typed
client operations deterministically; generation is reproducible.