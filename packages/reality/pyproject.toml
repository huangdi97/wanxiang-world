[project]
name = "wanxiang-reality"
version = "0.1.0"
description = "Wanxiang R7 versioned reality semantics: service contracts, RealityProfile/WorldProfile/RuntimeLock"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wanxiang_reality"]
