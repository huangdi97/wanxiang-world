{
  "name": "@wanxiang/cordis-host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Wanxiang composition runtime host built on Cordis (R7). Holds a capability graph only; canonical world state stays behind the Python authority seam.",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "cordis": "4.0.0-rc.10"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "@types/node": "^22.7.0",
    "eslint": "^9.9.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.3.0",
    "vitest": "^2.1.0"
  }
}
