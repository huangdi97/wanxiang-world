[project]
name = "wanxiang-domain"
version = "0.1.0"
description = "Wanxiang domain core: pure semantic contracts and invariants"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wanxiang_domain"]
