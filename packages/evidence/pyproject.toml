[project]
name = "wanxiang-evidence"
version = "0.1.0"
description = "Wanxiang evidence/rights core contracts and policies"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wanxiang_evidence"]
