[project]
name = "wanxiang-execution"
version = "0.1.0"
description = "Wanxiang isolated execution fabric and irreversible external-effect outbox"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wanxiang_execution"]
