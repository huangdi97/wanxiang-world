# Goal G38B Acceptance Report — Kernel v1 ABI 清单

## Status
PASS (mechanism) — kernel v1 ABI enumerated + golden fixtures frozen.

## Delivered
1. `packages/substrate/src/wanxiang_substrate/kernel/abi.py` (new):
   - `KERNEL_V1_ABI` — stable kernel v1 surface (ids/versions/commit kinds/
     event/snapshot/branch/lineage/worldline/constitution/replay/authority/
     ledger/promotion).
   - `abi_manifest()` / `abi_golden()` / `verify_abi_golden()` — deterministic
     golden hash + JSON snapshot.
2. `reports/kernel_v1_abi_golden.json` — frozen golden (generated by test).
3. `tests/unit/substrate/test_kernel_abi.py` (3 tests): stable manifest;
   reproducible golden hash; round-trip persistence + tamper detection.

## Verification
| Command | Result |
|---|---|
| `uv run pytest tests/unit/substrate/test_kernel_abi.py -q` | 3 passed |
| ruff / format / pyright | PASS / PASS / 0 errors |

## Local commit
- `g38b: Kernel v1 ABI 清单`
