# M95 / G98A — Stable baseline and ledger

**Conclusion:** `PASS` for G98A / Gate 61 only. This report does not accept
the human-player gates and does not unlock Stable release.

## Reproducible evidence

- Branch created from the verified closure descendant:
  `release/v5.5-stable-certification`.
- `HEAD`: `bf30d16472af1bb179a37239d8bc9eaccbb3cb2a` at baseline audit time.
- `v5.5.0-rc1` tag object: `e4768f706f427b3356c94d1e0ec3afe79c17987a`;
  peeled commit: `50443c8f7cc58fc4661af2a7d993dc7ec6144e11`.
- `v5.4.0` tag object: `8b9541ad21a8dda5c3f159fe72186431422a3a06`;
  peeled commit: `ef935fc6c24eb47553382d318e1501a795c4da84`.
- Final Closure commit `d98d90c927d3325dbe527112ddd14867b19c2177` and both
  release tag commits are ancestors of the current HEAD.
- `uv run python scripts/architecture_check.py`: PASS.
- `uv run python scripts/kernel_guard.py`: PASS.
- `uv run python scripts/playable_e2e.py`: PASS.
- `uv run python scripts/studio_socket_smoke.py`: PASS.
- `uv run python scripts/release_build.py`: PASS; migration head
  `0004_add_world_metadata`.
- Focused replay/branch/API/Playable/release/matrix tests: PASS when run as
  separate bounded commands; the earlier combined collection was interrupted
  after it stopped making progress and is not counted as a pass.

The machine-readable command output, hashes, parsed smoke results, tag facts,
and scope scan are in
`artifacts/v55_stable/baseline.json`, generated by
`uv run python scripts/stable_baseline.py`.

## Boundary and blockers

`IMPLEMENTED`: baseline probes and the existing RC1 product chain.

`VALIDATED`: Gate 61 lineage/scope freeze and the local deterministic baseline.

`EXPERIMENTAL` / `NOT_PROVEN`: Gates 62–80 are not inferred from inherited RC
claims. Human-player acceptance remains a separate evidence step.

`EXTERNAL_BLOCKED`: `git fetch --tags --prune` could not refresh the remote
because the host Git installation lacks its HTTPS remote helper. The exact
v5.5 master filename supplied in the execution prompt was not present in the
checkout; the available normative spec and v5.5 package were used only as
identified inputs, with the missing file retained as an explicit limitation.

No tag, GitHub release, remote push, historical report, v5.4 tag, canonical
history, or source asset was modified by G98A.
