# G66G Report — Domain Validation Sandbox

**PASS (application-level reference sandbox)** — candidate domain/capability
declarations are validated deterministically with a hash and violations. The
implementation does not claim OS-level isolation.

Evidence: undeclared capability failure and declared capability pass.
