# G64B Report — Cross-source Alignment

**PASS** — deterministic semantic-key grouping exposes aligned candidate ids
and source ids across editions while preserving each candidate payload.

Evidence: `FusionResult.alignments` assertions for two identity editions.
